{
  "name": "puntajeeventos",
  "version": "1.0.0",
  "description": "Organizar puntaje en eventos de competición",
  "main": "index.js",
  "engines": {
    "node": "20.15.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "build": "cd server && npm install && cd ../client && npm install && npm run build && cd ../login && npm install && npm run build",
    "dev": "npm run build && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
